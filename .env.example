# Copy to .env and fill in your values

# Database (PostgreSQL) - required for Neon, Vercel Postgres, Supabase, etc.
# For Neon/Vercel Postgres: use pooled URL for DATABASE_URL, direct URL for DIRECT_URL
# For single-connection providers: set both to the same connection string
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
DIRECT_URL=postgresql://user:password@host:5432/database?sslmode=require

# NextAuth
NEXTAUTH_SECRET=generate-a-random-string-at-least-32-chars
NEXTAUTH_URL=http://localhost:3000

# Character recommendations - emails are sent to this address (never exposed in the app)
RECOMMENDATIONS_EMAIL=your@email.com

# Resend API key for sending recommendation emails (https://resend.com)
RESEND_API_KEY=re_your_key

# Optional: Custom "from" email for Resend (must be verified domain)
# RESEND_FROM_EMAIL=Grand Line Exchange <noreply@yourdomain.com>
